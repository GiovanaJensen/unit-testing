import { Routes } from '@angular/router';
import { CreateNewAccessPlaceComponent } from './screens/create-new-access-place/create-new-access-place.page';

export const routes: Routes = [
    {
        path: '',
        component: CreateNewAccessPlaceComponent,
      },
];